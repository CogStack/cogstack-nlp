services:
  medcatweb:
    build:
      context: ./webapp
    volumes:
      - ../medcat/tests/resources:/webapp/models
    ports:
      - "8000:8000"
    env_file:
      - ./envs/env_medmen_test
    tty: true
